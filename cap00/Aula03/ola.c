// incluir bibliotecas
// # eh padrao para informar instruçoes para pre processamento, estilo shellScript
// include palavra chave da linguagem para incluir as bibliotecas, provavelmente igual import
// std significa standard provavelmente sao os built ins do python
// io input output talvez seja como javaScript q nao inclui o input automaticamente
// lib library nao faço nem ideia
// .h por enquanto outro misterio
// referencias a cima caso volte a olhar esse codigo e tirar os provavelmentes

#include <stdio.h>
#include <stdlib.h>

// int pq devemos indicar o retorno da funçao
// main(void) chamada da funçao padrao com parametro nulo ou nada iremos descobrir exatamente
// a linguagem nao identada, o bloco de comandos eh separado pelas {} e instruçoes ; 
int main( void ){

// printf() funçao sendo chamada normalmente inclusive quebra de linhas q eu inclui testando    
    printf( "HELLO WORLD!!!\n" ); 
// print extra pq fiquei curioso com esses espaços na formataçao    
    printf("OLA MUNDO!!!\n"); 
// return outra coisa q parece ser padrao o 0 ou seja limpo sem erros    
    return 0; 
}
