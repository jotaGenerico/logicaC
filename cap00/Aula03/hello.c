#include <stdio.h>     // inclui a biblioteca standard input output
#include <stdlib.h>    // inclui a biblioteca standard library

// função main (principal)
int main( void ) {
    
    // imprime na saída a string (texto) Hello, World!
    printf( "Hello, World!\n" );
    
    // retorna 0 ao sistema operacional
    return 0;
    
}
